function sendNotification() {
  //get active spreadsheet
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var sheet = ss.getActiveSheet();
  
  //get active cell
  var mycell = ss.getActiveSelection();
  var cellcol = mycell.getColumn();
  var cellrow = mycell.getRow();
  
  //get range
  var dataRange = sheet.getRange("P9:P13");
  //get data from the range
  var data = dataRange.getValues();
  
  
  //define notification details
  var recipient1 = "ocotterell@classoneequipment.com";
  var subject = "Notification for " +ss.getName();
  var body = "This is an automatic notification for " + ss.getName();
  
  //check if a part is below minimum value
  //check if in the Priority 1 Raider L/R range 
  if((cellcol >= 17 && cellcol <= 29) && sheet.getName() == "Inventory"){
    if((cellrow == 9) && (mycell.getValue() <= data[0])){      
      MailApp.sendEmail(recipient1, subject + " - Raider - Lift Motor Alter!", body + " lift motor quantity is low " 
                            + " visit the following link to see changes: " + ss.getUrl());
	}
	if((cellrow == 10) && (mycell.getValue() <= data[1])){
	  MailApp.sendEmail(recipient1, subject + " - Raider - Rotate Motor Alter!", body + " rotate motor quantity is low " 
                            + " visit the following link to see changes: " + ss.getUrl());	
	}
	if((cellrow == 11) && (mycell.getValue() <= data[2])){
	  MailApp.sendEmail(recipient1, subject + " - Raider - Ci-Coil Alter!", body + " Ci-Coil quantity is low " 
                            + " visit the following link to see changes: " + ss.getUrl());	
	}
	if((cellrow == 12) && (mycell.getValue() <= data[3])){
	  MailApp.sendEmail(recipient1, subject + " - Raider - Linear Actuator Alter!", body + " Linear Actuator quantity is low " 
                            + " visit the following link to see changes: " + ss.getUrl());	
	}
	if((cellrow == 13) && (mycell.getValue() <= data[4])){
	  MailApp.sendEmail(recipient1, subject + " - Raider - Harmonic Drive Alter!", body + " Harmonic Drive quantity is low " 
                            + " visit the following link to see changes: " + ss.getUrl());	
	}
  }
}

